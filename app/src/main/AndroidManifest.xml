<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="qualcar.com.qualcar" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".profile_activity"
            android:label="QualCar profile">
        </activity>
        <activity
            android:name=".AddRuleActivity"
            android:label="QualCar add rule">
        </activity>
        <activity
            android:name=".view.graph_activity"
            android:label="@string/title_activity_graph_activity"
            android:parentActivityName=".MainScreen" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="qualcar.com.qualcar.MainScreen" />
        </activity>
    </application>

</manifest>
